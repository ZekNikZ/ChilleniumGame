// SHAPE: shapes used by player
enum SHAPE {
	SQUARE,
	TRIANGLE,
	CIRCLE,
	CHEVRON
}

// MOVE_DIR: movement directions
enum MOVE_DIR {
	VERTICAL,
	HORIZONTAL
}

// TRANSITION_STATE: transition states
enum TRANSITION_STATE {
	OFF,
	NEXT,
	GOTO,
	RESTART,
	INTRO
}

// COLOR: color names
enum COLOR {
	WHITE,
	RED,
	YELLOW,
	BLUE,
	GREEN
}