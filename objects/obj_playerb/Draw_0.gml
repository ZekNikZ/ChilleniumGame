/// @description Change Shape Sprite
// Sprite Change 

draw_self()

if (shape == shape_change.Square)
{
	draw_sprite(spr_square,0,x,y);
}

if (shape == shape_change.Triangle)
{
		draw_sprite(spr_triangle,0,x,y);
}
